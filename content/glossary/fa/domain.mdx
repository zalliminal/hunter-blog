---
slug: "domain"
term: "نام دامنه"
abbreviation: ""
shortDefinition: "آدرس قابل خواندن برای انسان مانند example.com که به یک آدرس IP نگاشت می‌شود و به خاطر سپردن مکان وب‌سایت‌ها را آسان‌تر می‌کند."
category: "مبانی وب"
tags: ["دامنه", "dns", "وب", "نام-میزبان", "اینترنت"]
relatedTerms: ["dns", "url", "subdomain", "ip-address"]
language: "fa"
hasTranslation: true
translationSlug: "domain"
difficulty: "beginner"
lastUpdated: "2026-02-26"
sources:
  - "https://www.cloudflare.com/learning/dns/glossary/what-is-a-domain-name/"
---

## تعریف کوتاه
دامنه نام قابل خواندن یک وب‌سایت است، مثل google.com یا github.com. به جای تایپ 172.217.164.78، شما google.com را تایپ می‌کنید. DNS نام دامنه را به آدرس‌های IP که کامپیوترها واقعاً استفاده می‌کنند ترجمه می‌کند.

## تعریف کامل
نام دامنه یک شناسه قابل خواندن برای انسان برای یک مکان در اینترنت است. بخشی از یک سیستم سلسله‌مراتبی است که وب را قابل استفاده می‌کند.

**ساختار:**
```
subdomain.example.com
│         │      │
│         │      └─ TLD (دامنه سطح بالا)
│         └─ دامنه سطح دوم
└─ زیردامنه (اختیاری)

مثال‌های کامل:
www.example.com
api.example.com
blog.subdomain.example.com
```

**سطوح:**
- **TLD**: .com، .org، .io، .gov
- **سطح دوم**: example (در example.com)
- **زیردامنه**: www، api، mail

**چگونه کار می‌کند:**
۱. کاربر دامنه را در مرورگر تایپ می‌کند
۲. جستجوی DNS به IP ترجمه می‌کند
۳. مرورگر به آن IP متصل می‌شود
۴. سرور پاسخ می‌دهد

## چرا مهم است
- دامنه‌ها نحوه پیدا کردن وب‌سایت‌ها توسط کاربران است
- اعتماد مرتبط با نام دامنه
- گواهی‌ها به دامنه‌ها متصل هستند (HTTPS)
- دامنه‌ها در سیاست‌های امنیتی استفاده می‌شوند (CORS، CSP)
- مالکیت دامنه = کنترل

## چگونه مهاجمان از آن استفاده می‌کنند
**تکنیک‌های حمله:**

**۱. تایپ‌اسکواتینگ (typosquatting):**
```
google.com   ← قانونی
gooogle.com  ← جعلی (o اضافی)
g00gle.com   ← جعلی (صفر به جای 'o')
```

**۲. تصاحب زیردامنه:**
```
# شرکت زیردامنه ایجاد می‌کند
old-app.company.com → به AWS S3 اشاره می‌کند

# شرکت باکت S3 را حذف می‌کند اما DNS را فراموش می‌کند
# مهاجم باکتی با همان نام ایجاد می‌کند
old-app.company.com → اکنون تحت کنترل مهاجم است
```

**۳. DNS Rebinding (SSRF):**
```
۱. attacker.com → 1.2.3.4 (سرور مهاجم)
۲. اعتبارسنج می‌بیند: 1.2.3.4 ✓
۳. attacker.com → 169.254.169.254 (DNS تغییر کرد)
۴. درخواست واقعی به: 169.254.169.254 می‌رود
```

**۴. حملات هموگراف:**
```
аpple.com  ← از 'а' سیریلیک استفاده می‌کند، نه 'a' لاتین
apple.com  ← دامنه واقعی
```

یکسان به نظر می‌رسند اما دامنه‌های متفاوت هستند.

**۵. سرقت کوکی از طریق زیردامنه:**
```
# کوکی روی .example.com تنظیم شده
# مهاجم به خطر می‌اندازد: evil.example.com
# می‌تواند کوکی‌های همه زیردامنه‌ها را بدزدد
```

## چگونه تشخیص یا پیشگیری کنیم
**پیشگیری:**

**برای سازمان‌ها:**
- ثبت تایپ‌های رایج
- نظارت بر دامنه‌های مشابه
- استفاده از HTTPS با HSTS
- پیاده‌سازی رکوردهای CAA DNS
- نظارت بر گزارش‌های شفافیت گواهی
- حذف رکوردهای DNS آویزان
- استفاده از سرویس‌های نظارت دامنه

**برای برنامه‌ها:**
- اعتبارسنجی دامنه در برابر لیست سفید
- بررسی آدرس IP حل شده
- استفاده از دامنه‌های پین شده برای درخواست‌های حیاتی
- پیاده‌سازی صحیح CORS
- تنظیم دامنه‌های خاص کوکی

**نمونه اعتبارسنجی:**
```python
from urllib.parse import urlparse

ALLOWED_DOMAINS = ['example.com', 'api.example.com']

def is_safe_domain(url):
    parsed = urlparse(url)
    domain = parsed.hostname
    
    # تطابق دقیق
    if domain in ALLOWED_DOMAINS:
        return True
    
    # بررسی اینکه زیردامنه دامنه مجاز است
    for allowed in ALLOWED_DOMAINS:
        if domain.endswith('.' + allowed):
            return True
    
    return False
```

**تشخیص:**
- نظارت بر درخواست‌های DNS برای دامنه‌های غیرعادی
- بررسی گزارش‌های شفافیت گواهی
- استفاده از سرویس‌های اعتبار دامنه
- هشدار در مورد دامنه‌های مشابه تازه ثبت شده
- ردیابی تلاش‌های شمارش زیردامنه

## باورهای غلط رایج
- "زیردامنه = مالکیت جداگانه" - مالک یکسان همه را کنترل می‌کند
- "HTTPS = دامنه قانونی" - مهاجمان نیز گواهی می‌گیرند
- ".com امن‌تر از .io است" - TLD امنیت را تعیین نمی‌کند
- "دامنه در URL واضح است" - حملات هموگراف وجود دارد
- "نمی‌توان دامنه را جعل کرد" - تایپ‌اسکواتینگ و هموگراف کار می‌کنند

## مثال واقعی
**تایپ‌اسکواتینگ ورود Steam**

دامنه‌های جعلی:
```
steamcommunity.com   ← واقعی
steamcommunitу.com   ← جعلی (у سیریلیک)
steamcommunity.co    ← جعلی (.co به جای .com)
steam-community.com  ← جعلی (خط تیره)
```

نتیجه: هزاران اعتبارنامه دزدیده شده

**تصاحب زیردامنه GitHub (چندین مورد)**

۱. شرکت ایجاد می‌کند: `docs.company.com` → GitHub Pages
۲. شرکت مخزن GitHub را حذف می‌کند
۳. رکورد DNS باقی می‌ماند: `docs.company.com` → URL قدیمی GitHub
۴. مهاجم مخزنی با همان نام ایجاد می‌کند
۵. مهاجم اکنون `docs.company.com` را کنترل می‌کند

موارد واقعی: Uber، Shopify، Tesla و دیگران

**حمله هموگراف PayPal**

```
paypal.com   ← واقعی (حروف لاتین)
pаypаl.com   ← جعلی ('а' سیریلیک)
```

مرورگر ممکن است جعلی را در نوار آدرس واقعی نشان دهد.

## اصطلاحات مرتبط
DNS, URL, Subdomain, IP Address, Certificate

================================================================================
[ادامه اصطلاحات باقی‌مانده با همین قالب...]

آیا می‌خواهید با اصطلاحات باقی‌مانده (۱۳-۲۰) ادامه دهم؟